{% extends 'base.html.twig' %}

{% block title %}{{ 'services.page_title'|trans({}, 'services') }}{% endblock %}

{% block meta_description %}{{ 'services.meta.description'|trans({}, 'services') }}{% endblock %}

{% block meta_keywords %}{{ 'services.meta.keywords'|trans({}, 'services') }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">    <style>
    /* Services Styles - Inline until build issue is resolved */
    :root {
        --primary-gradient: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
        --secondary-gradient: linear-gradient(135deg, #34495e, #2c3e50);
        --card-gradient: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
        --icon-gradient: linear-gradient(135deg, #ecf0f1, #bdc3c7);
        --content-gradient: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
        
        --primary-color: #34495e;
        --secondary-color: #2c3e50;
        --text-muted: #64748b;
        --light-gray: #e2e8f0;
        
        --font-light: 200;
        --font-normal: 300;
        --font-medium: 500;
        --font-semibold: 600;
        
        --border-radius-lg: 20px;
        --border-radius-md: 0.5rem;
        --transition-smooth: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        --transition-fast: all 0.3s ease;
    }
    
    .services-hero {
        background: var(--primary-gradient);
        color: white;
        padding: 8rem 0 6rem;
        margin-top: 76px;
        position: relative;
        overflow: hidden;
        text-align: left;
    }

    .services-hero::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: repeating-linear-gradient(
            90deg,
            transparent,
            transparent 80px,
            rgba(255, 255, 255, 0.02) 80px,
            rgba(255, 255, 255, 0.02) 81px
        );
        pointer-events: none;
    }    .services-hero .section-header {
        text-align: left !important;
        max-width: 800px;
        margin: 0;
        position: relative;
        z-index: 2;
    }    .services-hero h1 {
        font-size: clamp(2.5rem, 5vw, 3.5rem);
        font-weight: var(--font-light);
        margin-bottom: 1.5rem;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        color: white;
        letter-spacing: -0.02em;
        line-height: 1.1;
        text-align: left !important;
    }    .services-hero p {
        font-size: 1.2rem;
        opacity: 0.9;
        line-height: 1.7;
        margin: 0;
        color: white;
        max-width: 600px;
        font-weight: var(--font-normal);
        text-align: left !important;
    }

    .services-content {
        background: var(--content-gradient);
        padding: 6rem 0;
    }.services-content .section-header {
        text-align: left !important;
        margin-bottom: 4rem;
    }    .services-content h2 {
        font-size: clamp(2rem, 4vw, 2.5rem);
        font-weight: var(--font-light);
        color: var(--secondary-color);
        margin-bottom: 1rem;
        position: relative;
        text-align: left !important;
    }

    .services-content p {
        font-size: 1.1rem;
        color: var(--text-muted);
        max-width: 600px;
        margin: 0;
        line-height: 1.6;
        font-weight: var(--font-normal);
        text-align: left !important;
    }    .service-card {
        background: var(--card-gradient);
        border-radius: var(--border-radius-lg);
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        transition: var(--transition-smooth);
        border: 1px solid var(--light-gray);
        position: relative;
    }

    .service-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: var(--secondary-gradient);
        transform: scaleX(0);
        transition: transform 0.4s ease;
        transform-origin: left;
    }

    .service-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.12);
        border-color: var(--primary-color);
    }

    .service-card:hover::before {
        transform: scaleX(1);
    }

    .service-card:hover .service-icon {
        background: var(--secondary-gradient);
        color: white;
        transform: scale(1.1);
    }

    .service-card-inner {
        padding: 2.5rem;
        height: 100%;
        display: flex;
        flex-direction: column;
        text-align: center;
    }

    .service-icon-wrapper {
        margin-bottom: 2rem;
    }    .service-icon {
        font-size: 3.5rem;
        color: var(--primary-color);
        background: var(--icon-gradient);
        width: 80px;
        height: 80px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: var(--transition-fast);
    }

    .service-content {
        flex-grow: 1;
        margin-bottom: 2rem;
    }

    .service-title {
        font-size: 1.5rem;
        font-weight: var(--font-semibold);
        color: var(--secondary-color);
        margin-bottom: 1rem;
        line-height: 1.4;
    }

    .service-description {
        color: var(--text-muted);
        line-height: 1.6;
        margin-bottom: 1.5rem;
        font-weight: var(--font-normal);
    }

    .service-features .feature-list {
        list-style: none;
        padding: 0;
        margin: 0;
        text-align: left;
    }    .service-features .feature-list li {
        display: flex;
        align-items: center;
        color: var(--text-muted);
        margin-bottom: 0.75rem;
        font-size: 0.95rem;
        font-weight: var(--font-medium);
        gap: 0.75rem;
    }

    .service-features .feature-list li .material-icons {
        font-size: 1.2rem;
        color: var(--primary-color);
        flex-shrink: 0;
    }

    .service-action {
        margin-top: auto;
        text-align: center;
    }    .services-cta {
        background: var(--primary-gradient);
        color: white;
        padding: 6rem 0;
        position: relative;
        text-align: left !important;
    }

    .services-cta h2,
    .services-cta p {
        text-align: left !important;
    }

    .services-cta::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: repeating-linear-gradient(
            45deg,
            transparent,
            transparent 2px,
            rgba(255, 255, 255, 0.02) 2px,
            rgba(255, 255, 255, 0.02) 4px
        );
        pointer-events: none;
    }    .btn-industrial {
        background: var(--secondary-gradient);
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: var(--border-radius-md);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        transition: var(--transition-fast);
        font-weight: var(--font-medium);
    }

    .btn-industrial:hover {
        background: linear-gradient(135deg, var(--secondary-color), #1a252f);
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    .btn-cta {
        font-size: 1.1rem;
        padding: 1rem 2rem;
    }
    </style>
{% endblock %}

{% block body %}
    <!-- Hero Section -->
    <section class="services-hero">
        <div class="container">            <div class="section-header">
                <h1>{{ 'services.hero.title'|trans({}, 'services') }}</h1>
                <p>{{ 'services.hero.subtitle'|trans({}, 'services') }}</p>
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services-content py-5">
        <div class="container">            <div class="section-header mb-5">
                <h2>{{ 'services.section.title'|trans({}, 'services') }}</h2>
                <p>{{ 'services.section.subtitle'|trans({}, 'services') }}</p>
            </div>

            <div class="row g-4">
                {% for service in services %}
                    <div class="col-lg-4 col-md-6">
                        <div class="service-card h-100">
                            <div class="service-card-inner">
                                <div class="service-icon-wrapper">
                                    <i class="material-icons service-icon">{{ service.icon|default('business') }}</i>
                                </div>                                <div class="service-content">
                                    <h3 class="service-title">{{ service.title|trans({}, 'services') }}</h3>
                                    <p class="service-description">{{ service.description|trans({}, 'services') }}</p>
                                    <div class="service-features">
                                        {% if service.features is defined %}
                                            <ul class="feature-list">
                                                {% for feature in service.features %}
                                                    <li><i class="material-icons">check_circle</i> {{ feature|trans({}, 'services') }}</li>
                                                {% endfor %}
                                            </ul>
                                        {% endif %}
                                    </div>
                                </div>                                <div class="service-action">
                                    <a href="{{ path('app_contact') }}" class="btn btn-industrial">
                                        <i class="material-icons">contact_mail</i>
                                        <span>{{ 'services.contact_button'|trans({}, 'services') }}</span>
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="services-cta">
        <div class="container">                <div class="cta-content">
                <div class="row align-items-center">
                    <div class="col-lg-8">
                        <h2>{{ 'services.cta.title'|trans({}, 'services') }}</h2>
                        <p>{{ 'services.cta.subtitle'|trans({}, 'services') }}</p>
                    </div>
                    <div class="col-lg-4 text-lg-end">
                        <a href="{{ path('app_contact') }}" class="btn btn-industrial btn-cta">
                            <i class="material-icons">phone</i>
                            <span>{{ 'services.cta.button'|trans({}, 'services') }}</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}