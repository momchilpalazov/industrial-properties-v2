{% extends 'admin/base.html.twig' %}

{% block title %}Управление на типове имоти{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .category-row {
            transition: background-color 0.2s;
        }
        
        .category-row:hover {
            background-color: #f5f5f5;
        }
        
        .category-level-0 {
            background-color: #f8f9fa;
            font-weight: 600;
        }
        
        .category-level-1 {
            background-color: #ffffff;
        }
        
        .category-level-2 {
            background-color: #ffffff;
        }
        
        .category-level-3 {
            background-color: #ffffff;
        }
        
        .category-name {
            display: flex;
            align-items: center;
        }
        
        .category-indent {
            display: inline-block;
            width: 20px;
            height: 1px;
        }
        
        .category-icon {
            margin-right: 0.5rem;
            color: #6c757d;
        }
        
        .category-badge {
            font-size: 0.8rem;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            margin-right: 0.5rem;
        }
        
        .level-0-badge {
            background-color: #0d6efd;
            color: white;
        }
        
        .level-1-badge {
            background-color: #6c757d;
            color: white;
        }
        
        .level-2-badge {
            background-color: #198754;
            color: white;
        }
        
        .level-3-badge {
            background-color: #dc3545;
            color: white;
        }
        
        .properties-count {
            display: inline-block;
            min-width: 1.5rem;
            padding: 0.2rem 0.4rem;
            font-size: 0.75rem;
            font-weight: 700;
            line-height: 1;
            text-align: center;
            white-space: nowrap;
            vertical-align: baseline;
            border-radius: 0.25rem;
        }
        
        .properties-count-main {
            background-color: #0d6efd;
            color: white;
        }
        
        .properties-count-sub {
            background-color: #6c757d;
            color: white;
        }

        .subcategory-count {
            font-weight: normal;
            font-size: 0.85rem;
            color: #6c757d;
            margin-left: 0.5rem;
        }
        
        .category-hidden {
            opacity: 0.5;
        }
        
        .action-buttons {
            display: flex;
            gap: 5px;
        }
        
        .action-button {
            padding: 0.25rem 0.5rem;
            font-size: 0.75rem;
        }
        
        .tree-indent {
            display: inline-block;
            width: 24px;
            position: relative;
        }
        
        .tree-indent:after {
            content: "";
            position: absolute;
            left: 12px;
            top: 0;
            bottom: 0;
            width: 1px;
            background-color: #dee2e6;
        }
        
        .tree-branch {
            display: inline-block;
            width: 24px;
            position: relative;
        }
        
        .tree-branch:before {
            content: "";
            position: absolute;
            left: 0;
            top: 50%;
            width: 12px;
            height: 1px;
            background-color: #dee2e6;
        }
        
        .tree-branch:after {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            height: 50%;
            width: 1px;
            background-color: #dee2e6;
        }
        
        .category-name-text {
            font-weight: 500;
            margin-left: 5px;
        }
        
        .category-level-0 .category-name-text {
            font-weight: 600;
        }
        
        .toggle-icon {
            cursor: pointer;
            width: 24px;
            height: 24px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-right: 5px;
            color: #6c757d;
            transition: all 0.2s;
        }
        
        .toggle-icon:hover {
            color: #0d6efd;
        }
        
        .subcategories {
            display: none;
        }
        
        .subcategories.show {
            display: table-row-group;
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Функция за превключване на видимостта на подкатегориите
            function toggleSubcategories(event) {
                const toggleIcon = event.currentTarget;
                const categoryId = toggleIcon.getAttribute('data-category-id');
                const subcategoriesContainer = document.getElementById('subcategories-' + categoryId);
                
                if (subcategoriesContainer) {
                    subcategoriesContainer.classList.toggle('show');
                    
                    // Промяна на иконата
                    if (subcategoriesContainer.classList.contains('show')) {
                        toggleIcon.innerHTML = '<i class="bi bi-dash-square"></i>';
                    } else {
                        toggleIcon.innerHTML = '<i class="bi bi-plus-square"></i>';
                    }
                }
            }
            
            // Добавяне на слушатели за събития към всички бутони за превключване
            const toggleIcons = document.querySelectorAll('.toggle-icon');
            toggleIcons.forEach(icon => {
                icon.addEventListener('click', toggleSubcategories);
            });
            
            // Автоматично разгъване на всички категории при зареждане
            toggleIcons.forEach(icon => {
                icon.click();
            });
        });
    </script>
{% endblock %}

{% block body %}
    <div class="container-fluid px-4">
        <h1 class="mt-4">Типове имоти</h1>
        
        <div class="card mb-4">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <i class="bi bi-diagram-3 me-1"></i>
                        Категории и подкатегории
                    </div>
                    <a href="{{ path('admin_property_type_new') }}" class="btn btn-primary">
                        <i class="bi bi-plus-lg"></i> Добави нов тип
                    </a>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th width="60">ID</th>
                                <th>Категория</th>
                                <th>Описание</th>
                                <th>Имоти</th>
                                <th class="text-end">Действия</th>
                            </tr>
                        </thead>
                        
                        {% macro renderCategory(category, level) %}
                            <tr class="category-row category-level-{{ level }} {% if not category.isVisible %}category-hidden{% endif %}">
                                <td>{{ category.id }}</td>
                                <td>
                                    <div class="category-name">
                                        {% for i in 1..level %}
                                            <div class="tree-indent"></div>
                                        {% endfor %}
                                        
                                        {% if level > 0 %}
                                            <div class="tree-branch"></div>
                                        {% endif %}
                                        
                                        {% if category.children|length > 0 %}
                                            <span class="toggle-icon" data-category-id="{{ category.id }}">
                                                <i class="bi bi-plus-square"></i>
                                            </span>
                                        {% else %}
                                            <span style="width: 24px; display: inline-block;"></span>
                                        {% endif %}
                                        
                                        <span class="category-badge level-{{ level }}-badge">
                                            {% if level == 0 %}
                                                <i class="bi bi-folder"></i>
                                            {% elseif level == 1 %}
                                                <i class="bi bi-arrow-return-right"></i>
                                            {% elseif level == 2 %}
                                                <i class="bi bi-dash"></i>
                                            {% else %}
                                                <i class="bi bi-dot"></i>
                                            {% endif %}
                                        </span>
                                        
                                        <span class="category-name-text">{{ category.name }}</span>
                                        
                                        {% if category.children|length > 0 %}
                                            <span class="subcategory-count">
                                                <i class="bi bi-diagram-3"></i>
                                                {{ category.children|length }} подкатегор{{ category.children|length == 1 ? 'ия' : 'ии' }}
                                            </span>
                                        {% endif %}
                                        
                                        {% if not category.isVisible %}
                                            <span class="badge bg-warning ms-2">Скрит</span>
                                        {% endif %}
                                    </div>
                                </td>
                                <td>
                                    {% if category.description %}
                                        {{ category.description }}
                                    {% else %}
                                        <span class="text-muted">-</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% if category.properties|length > 0 %}
                                        <span class="properties-count properties-count-{{ level == 0 ? 'main' : 'sub' }}">
                                            {{ category.properties|length }}
                                        </span>
                                    {% else %}
                                        <span class="text-muted">0</span>
                                    {% endif %}
                                </td>
                                <td class="text-end">
                                    <div class="action-buttons">
                                        <form method="post" action="{{ path('admin_property_type_move_up', {'id': category.id}) }}" style="display: inline-block">
                                            <input type="hidden" name="_token" value="{{ csrf_token('move-up' ~ category.id) }}">
                                            <button type="submit" class="btn btn-sm btn-outline-secondary action-button" title="Премести нагоре">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        
                                        <form method="post" action="{{ path('admin_property_type_move_down', {'id': category.id}) }}" style="display: inline-block">
                                            <input type="hidden" name="_token" value="{{ csrf_token('move-down' ~ category.id) }}">
                                            <button type="submit" class="btn btn-sm btn-outline-secondary action-button" title="Премести надолу">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        
                                        <form method="post" action="{{ path('admin_property_type_toggle_visibility', {'id': category.id}) }}" style="display: inline-block">
                                            <input type="hidden" name="_token" value="{{ csrf_token('toggle-visibility' ~ category.id) }}">
                                            <button type="submit" class="btn btn-sm btn-outline-{{ category.isVisible ? 'warning' : 'success' }} action-button" title="{{ category.isVisible ? 'Скрий' : 'Покажи' }}">
                                                <i class="bi bi-{{ category.isVisible ? 'eye-slash' : 'eye' }}"></i>
                                            </button>
                                        </form>
                                        
                                        <a href="{{ path('admin_property_type_add_subcategory', {'id': category.id}) }}" 
                                           class="btn btn-sm btn-outline-info action-button" 
                                           title="Добави подкатегория">
                                            <i class="bi bi-plus-circle"></i>
                                        </a>
                                        
                                        <a href="{{ path('admin_property_type_edit', {'id': category.id}) }}" 
                                           class="btn btn-sm btn-outline-primary action-button" 
                                           title="Редактирай">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        
                                        {% if not category.hasChildren and category.properties|length == 0 %}
                                            <form method="post" 
                                                  action="{{ path('admin_property_type_delete', {'id': category.id}) }}" 
                                                  style="display: inline-block" 
                                                  onsubmit="return confirm('Сигурни ли сте, че искате да изтриете този тип имот?');">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
                                                <button type="submit" class="btn btn-sm btn-outline-danger action-button" title="Изтрий">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        {% else %}
                                            <button type="button" class="btn btn-sm btn-outline-secondary action-button" disabled title="Не може да бъде изтрит, защото има свързани подкатегории или имоти">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        {% endif %}
                                    </div>
                                </td>
                            </tr>
                            
                            {% if category.children|length > 0 %}
                                <tbody id="subcategories-{{ category.id }}" class="subcategories">
                                    {% for child in category.children|sort((a, b) => a.position <=> b.position) %}
                                        {{ _self.renderCategory(child, level + 1) }}
                                    {% endfor %}
                                </tbody>
                            {% endif %}
                        {% endmacro %}
                        
                        <tbody>
                            {% if root_categories|length > 0 %}
                                {% for category in root_categories %}
                                    {{ _self.renderCategory(category, 0) }}
                                {% endfor %}
                            {% else %}
                                <tr>
                                    <td colspan="5" class="text-center">Няма намерени типове имоти</td>
                                </tr>
                            {% endif %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        
        <div class="card mb-4">
            <div class="card-header">
                <i class="bi bi-info-circle me-1"></i>
                Информация за типовете имоти
            </div>
            <div class="card-body">
                <div class="alert alert-info alert-permanent">
                    <h5><i class="bi bi-lightbulb"></i> Как работят категориите?</h5>
                    <p>
                        <strong>Основна категория:</strong> Представлява главен тип имот (например "Под наем", "Продажба").<br>
                        <strong>Подкатегория:</strong> Представлява подтип на основна категория (например "Офис сграда" е подкатегория на "Под наем").<br>
                        <strong>Под-подкатегория:</strong> Представлява подтип на подкатегория (например "Луксозен офис" е под-подкатегория на "Офис сграда").
                    </p>
                    <p>
                        <strong>Указания:</strong>
                        <ul>
                            <li>Използвайте бутона <i class="bi bi-plus-square"></i>/<i class="bi bi-dash-square"></i> за разгъване/свиване на подкатегориите.</li>
                            <li>Използвайте бутона <i class="bi bi-plus-circle"></i> за добавяне на подкатегория към съществуваща категория.</li>
                            <li>Използвайте бутоните <i class="bi bi-arrow-up"></i> и <i class="bi bi-arrow-down"></i> за промяна на реда на категориите.</li>
                            <li>Използвайте бутона <i class="bi bi-eye-slash"></i> за скриване на категория (тя няма да се показва в публичната част).</li>
                            <li>Категория с подкатегории или свързани имоти не може да бъде изтрита.</li>
                        </ul>
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 