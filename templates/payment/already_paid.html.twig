{% extends 'base.html.twig' %}

{% block title %}Промоцията е вече платена{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .already-paid-container {
            max-width: 700px;
            margin: 0 auto;
            padding: 3rem 1rem;
            text-align: center;
        }
        
        .already-paid-icon {
            font-size: 4rem;
            color: #28a745;
            margin-bottom: 1.5rem;
        }
        
        .already-paid-title {
            font-size: 2rem;
            margin-bottom: 1rem;
            color: #333;
        }
        
        .already-paid-message {
            font-size: 1.1rem;
            color: #6c757d;
            margin-bottom: 2rem;
        }
        
        .already-paid-details {
            background-color: #f8f9fa;
            border-radius: 0.5rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: left;
        }
        
        .already-paid-actions {
            margin-top: 2rem;
        }
        
        .already-paid-actions .btn {
            padding: 0.75rem 1.5rem;
            font-size: 1.1rem;
            margin: 0 0.5rem;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="already-paid-container">
        <div class="already-paid-icon">
            <i class="bi bi-check-circle-fill"></i>
        </div>
        
        <h1 class="already-paid-title">Промоцията е вече платена</h1>
        
        <p class="already-paid-message">
            Тази промоция вече е успешно платена и активирана. Не е необходимо повторно плащане.
        </p>
        
        <div class="already-paid-details">
            <h4 class="mb-3">Детайли за промоцията</h4>
            <div class="row">
                <div class="col-md-6">
                    <p><strong>Имот:</strong> {{ promotion.property.titleBg }}</p>
                    <p><strong>Референтен номер:</strong> {{ promotion.property.reference }}</p>
                    <p>
                        <strong>Тип промоция:</strong>
                        {% if promotion.type == 'vip' %}
                            <span class="badge bg-warning">VIP</span>
                        {% else %}
                            <span class="badge bg-info">Избрана</span>
                        {% endif %}
                    </p>
                </div>
                <div class="col-md-6">
                    <p><strong>Начална дата:</strong> {{ promotion.startDate|date('d.m.Y') }}</p>
                    <p><strong>Крайна дата:</strong> {{ promotion.endDate|date('d.m.Y') }}</p>
                    <p>
                        <strong>Статус:</strong>
                        {% if promotion.isActive %}
                            <span class="badge bg-success">Активна</span>
                        {% else %}
                            <span class="badge bg-secondary">Изтекла</span>
                        {% endif %}
                    </p>
                </div>
            </div>
        </div>
        
        <div class="already-paid-actions">
            <a href="{{ path('app_property_show', {id: promotion.property.id}) }}" class="btn btn-primary">
                <i class="bi bi-building me-2"></i> Преглед на имота
            </a>
            <a href="{{ path('app_home') }}" class="btn btn-outline-secondary">
                <i class="bi bi-house me-2"></i> Към началната страница
            </a>
        </div>
    </div>
{% endblock %} 