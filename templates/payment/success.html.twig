{% if is_public %}
    {% extends 'base_public.html.twig' %}
{% else %}
    {% extends 'admin/base.html.twig' %}
{% endif %}

{% block title %}Успешно плащане{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .success-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 3rem 2rem;
            text-align: center;
        }
        
        .success-icon {
            font-size: 5rem;
            color: #28a745;
            margin-bottom: 2rem;
        }
        
        .success-title {
            font-size: 2rem;
            margin-bottom: 1rem;
        }
        
        .success-message {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            color: #6c757d;
        }
        
        .success-details {
            background-color: #f8f9fa;
            border-radius: 0.5rem;
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: left;
        }
        
        .success-actions {
            margin-top: 2rem;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="success-container">
        <div class="success-icon">
            <i class="bi bi-check-circle-fill"></i>
        </div>
        
        <h1 class="success-title">Плащането е успешно!</h1>
        <p class="success-message">Благодарим ви за плащането. Вашата промоция е активирана успешно.</p>
        
        <div class="success-details">
            <h4 class="mb-3">Детайли за промоцията</h4>
            
            <p><strong>Имот:</strong> {{ promotion.property.title }}</p>
            <p><strong>Референтен номер:</strong> {{ promotion.property.referenceNumber }}</p>
            <p>
                <strong>Тип промоция:</strong>
                {% if promotion.type == 'vip' %}
                    <span class="badge bg-warning">VIP обява</span>
                {% else %}
                    <span class="badge bg-info">Избрана обява</span>
                {% endif %}
            </p>
            <p><strong>Начална дата:</strong> {{ promotion.startDate|date('d.m.Y') }}</p>
            <p><strong>Крайна дата:</strong> {{ promotion.endDate|date('d.m.Y') }}</p>
            <p><strong>Транзакция:</strong> {{ promotion.transactionId }}</p>
        </div>
        
        <div class="success-actions">
            <a href="{{ path('admin_property_show', {id: promotion.property.id}) }}" class="btn btn-primary btn-lg">
                <i class="bi bi-building me-2"></i> Към имота
            </a>
            <a href="{{ path('admin_promotion_index') }}" class="btn btn-outline-secondary btn-lg ms-2">
                <i class="bi bi-list me-2"></i> Всички промоции
            </a>
        </div>
    </div>
{% endblock %} 